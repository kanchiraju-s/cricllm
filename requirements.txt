# Core LangChain stack (1.x line — current stable, non-deprecated APIs)
langchain==1.3.11
langchain-core==1.4.8
langchain-text-splitters==1.1.2
langchain-google-genai==4.2.6
langchain-experimental==0.4.2

# Vector store backend (used directly via the chromadb client, not through
# langchain-chroma, so pre-computed/cached embeddings upsert without recompute)
chromadb==1.5.9

# Google GenAI SDK (transitively required by langchain-google-genai 4.x —
# note this is the unified `google-genai` SDK, not the legacy
# `google-generativeai` package, which langchain-google-genai 4.x no longer uses)
google-genai==2.10.0

# Config / validation
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# Resilience, progress, encoding, tokens
tenacity==9.1.4
tqdm==4.68.3
charset-normalizer==3.4.7
tiktoken==0.13.0

# Testing
pytest==9.1.1
pytest-mock==3.15.1

# Web UI (app.py) — the model answers in Markdown, so we render it to HTML
# server-side, then sanitize before it ever hits innerHTML in the browser
flask==3.1.0
markdown==3.10.2
bleach==6.4.0
